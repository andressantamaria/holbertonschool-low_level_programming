#include "main.h"
/**
 * print_diagonal - draws a diagonal
 * @n: determine lenght
 */
void print_diagonal(int n)
{
	int j;

	if(n <= 0)
		_putchar('\n');
	else
	{
		for(j = 0; j <= n; j++)
		{
			_putchar(32);
		}
		_putchar(92);
		_putchar('\n');
	}
}
